#!/bin/bash

if [ -x /usr/bin/fortune ]; then
  if [ -f ~/.dzc/fortunes/one ]; then
    echo ""
    echo -e "`fortune ~/.dzc/fortunes/one`"
    echo ""
  elif [ -x /usr/bin/cowsay ]; then
    fortune | cowsay
  fi
fi

